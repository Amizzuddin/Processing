//Car mycar;                                      //declare mycar data type object as a global variable

/*class Car declaration*/
/*
class Car{
  //Variables
  color c;
  int xpos;
  int ypos;
  int direction;
  int speed;
  final int obj_dis = 20;
  
  //Constructor - need to be the same name as the class itself!
  Car(int _direction, int _speed){
    //(color _c, int _xpos, int _ypos, int _direction)
    //c = _c;
    //xpos = _xpos;
    //ypos = _ypos;
    direction = _direction;
    speed = _speed;
  }
  
  //update data from serial communication
  void update(int _data[]){
    direction = _data[0];
    speed = _data[1];
  }
  
  void display(){
    println(direction);
    //stroke(0);
    //fill(c);
    //ellipse(xpos, ypos, obj_dis, obj_dis);
    //fill(255);
    //textFont(f, obj_dis/2);    
    //text( "(" + xpos + "," + ypos + ")", xpos, ypos+obj_dis);
  }
  
}
*/
